# Core (required)
mcp>=1.0.0
sqlalchemy>=2.0
rank-bm25>=0.2
numpy>=1.24
fastapi>=0.100
uvicorn>=0.20
python-dotenv>=1.0
pyyaml>=6.0

# Semantic search (optional — BM25 works without this)
# Recommended (no GPU needed): pip install 'skills-lab[semantic-onnx]'
#   → onnxruntime + tokenizers + huggingface_hub (zero torch dependency)
# With GPU support:       pip install 'skills-lab[semantic]'
#   → sentence-transformers + torch
